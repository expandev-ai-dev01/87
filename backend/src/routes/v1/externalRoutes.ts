import { Router } from 'express';

const router = Router();

/**
 * @summary
 * External (public) routes configuration
 *
 * @description
 * Defines all public API endpoints that don't require authentication.
 * These routes are accessible at /api/v1/external/...
 *
 * @module externalRoutes
 */

// Public routes will be added here as features are implemented
// Example:
// import publicController from '@/api/v1/external/public/controller';
// router.get('/public/info', publicController.getHandler);

export default router;
